apply plugin: 'com.android.library'

android {
    compileSdkVersion 28
    buildToolsVersion "28.0.3"

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 27
    }
}

dependencies {
    provided 'com.facebook.react:react-native:+'
    compile 'com.amap.api:location:4.5.0'
}
